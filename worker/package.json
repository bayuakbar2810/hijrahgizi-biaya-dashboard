{
  "name": "hijrahgizi-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --outfile=dist/index.mjs --platform=browser --target=es2022",
    "test": "node scripts/test.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.80.0"
  }
}